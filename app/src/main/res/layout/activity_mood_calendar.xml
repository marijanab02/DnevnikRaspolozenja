<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#B3E5FC">

    <!-- CLOUD BACKGROUND -->
    <include
        android:id="@+id/cloudBackground"
        layout="@layout/cloud_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- CONTENT CONTAINER -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- GLAVNI SADRŽAJ -->
        <androidx.cardview.widget.CardView
            android:id="@+id/calendarCard"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            app:cardCornerRadius="20dp"
            app:cardElevation="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_bias="0.15">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="#FFFFFF"
                android:padding="16dp">

                <!-- HEADER -->
                <TextView
                    android:id="@+id/tvCalendarTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="📅 Kalendar Raspoloženja"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#0288D1"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginBottom="16dp"/>

                <!-- KONTROLE -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center"
                    android:layout_marginBottom="16dp"
                    android:background="@drawable/input_background"
                    android:padding="8dp">

                    <Button
                        android:id="@+id/btnPrev"
                        android:layout_width="56dp"
                        android:layout_height="56dp"
                        android:text="◀"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:backgroundTint="#0288D1"
                        android:textColor="#FFFFFF"/>

                    <TextView
                        android:id="@+id/tvMonthTitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Siječanj 2026"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:textColor="#01579B"
                        android:gravity="center"
                        android:layout_marginHorizontal="12dp"/>

                    <Button
                        android:id="@+id/btnNext"
                        android:layout_width="56dp"
                        android:layout_height="56dp"
                        android:text="▶"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:backgroundTint="#0288D1"
                        android:textColor="#FFFFFF"/>
                </LinearLayout>

                <!-- DANI -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginBottom="8dp"
                    android:weightSum="7">

                    <TextView style="@style/DayHeaderStyle" android:text="PON"/>
                    <TextView style="@style/DayHeaderStyle" android:text="UTO"/>
                    <TextView style="@style/DayHeaderStyle" android:text="SRI"/>
                    <TextView style="@style/DayHeaderStyle" android:text="ČET"/>
                    <TextView style="@style/DayHeaderStyle" android:text="PET"/>
                    <TextView style="@style/DayHeaderStyle" android:text="SUB" android:textColor="#0288D1"/>
                    <TextView style="@style/DayHeaderStyle" android:text="NED" android:textColor="#0288D1"/>
                </LinearLayout>

                <!-- KALENDAR GRID -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewCalendar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:nestedScrollingEnabled="false"/>
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- BACK BUTTON -->
        <Button
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="← Natrag"
            android:textSize="16sp"
            android:backgroundTint="#81D4FA"
            android:textColor="#FFFFFF"
            android:paddingHorizontal="32dp"
            android:paddingVertical="12dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
